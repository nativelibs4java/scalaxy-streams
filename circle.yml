dependencies:
  cache_directories:
    - ~/.m2
    - "~/.ivy2"
    - "~/.sbt"

test:
  override:
    - sbt 'set javaOptions += "-Djunit.parallel.threads=2"' 'set
  parallelExecution in Test := false' test
